#include <raylib.h>

// Raylib already has this function but i want to try a custom implementation to compare how badly it performs
RayCollision triangleCollisionCheck(Ray ray, Vector3 p1, Vector3 p2, Vector3 p3){

}